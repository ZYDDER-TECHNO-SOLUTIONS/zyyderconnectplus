apiVersion: v1
kind: Service
metadata:
  name: auth-service-external
  namespace: sparklex
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30081
---
apiVersion: v1
kind: Service
metadata:
  name: job-service-external
  namespace: sparklex
spec:
  type: NodePort
  selector:
    app: job-service
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30082
---
apiVersion: v1
kind: Service
metadata:
  name: resume-service-external
  namespace: sparklex
spec:
  type: NodePort
  selector:
    app: resume-service
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30083
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service-external
  namespace: sparklex
spec:
  type: NodePort
  selector:
    app: notification-service
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30084
